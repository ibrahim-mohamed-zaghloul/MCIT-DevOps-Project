apiVersion: v1
kind: Service
metadata:
  name: svc-backend
  namespace: dev
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30002  # Assign a NodePort for external access
